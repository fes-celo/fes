---
/**
 * Stand-in for photography not shot or selected yet. Token-tinted block
 * (no real asset dependency) so a page can ship layout-complete ahead of
 * final photography. The alt text says what belongs here.
 * TODO(marcelo): replace every usage with a real <Image> once assets land.
 */
interface Props {
  alt: string
  tone?: 'accent' | 'clay' | 'neutral' | 'tint'
  class?: string
}

const { alt, tone = 'accent', class: className } = Astro.props

const toneClasses: Record<string, string> = {
  accent: 'bg-accent-100',
  clay: 'bg-clay-100',
  neutral: 'bg-neutral-200',
  // The only theme-aware tone: `--color-accent-tint` is a semantic token the
  // Systems pages' `.dark-zone`/`.reveal-zone` re-point at neutral-900, so a
  // placeholder sitting inside one of those zones goes dark with it instead
  // of staying a fixed pale blue. Use it wherever the placeholder fills a
  // `bg-accent-tint` image well (ProjectCard's featured variant).
  tint: 'bg-accent-tint',
}
---

<div role="img" aria-label={alt} class:list={[toneClasses[tone], className]} />
